struct Point {
    x: u32,
    y: u32
}

expand S {
    df new() S {
        return S {
            x: 5,
            y: 5
        };
    }

    df add(self: Point, rhs: Point) Point {
        return Point {
            x: self.x + rhs.x,
            y: self.y + rhs.y
        };
    }
}