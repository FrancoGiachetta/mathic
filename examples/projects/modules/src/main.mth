imp util;
imp util::mul;
imp types::Point;
imp geo::distance;

df main() i64 {
    let p1: Point = types::Point { x: 3, y: 4 };
    let p2: Point = types::Point { x: 6, y: 8 };
    let d: i64 = geo::distance(p1, p2);
    let area: i64 = mul(p1.x, p1.y);
    return util::add(d, area);
}