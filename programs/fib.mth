df main() {
	return fib(10);
}

df fib(number) {
	let val = 0;

	if (number <= 0) {
		val = 0;
	} else {
		if (number == 1) {
			val = 1;
		} else {
			val = fib(number - 1) + fib(number - 2);
		}
	}

	return val;
}
