df main() {
	return fib(10);
}

df fib(number) {
	let return_val = 0;

	if (number <= 0) {
		return_val = 0;
	} else {
		if (number == 1) {
			return_val = 1;
		} else {
			return_val = fib(number - 1) + fib(number - 2);
		}
	}

	return return_val;
}
