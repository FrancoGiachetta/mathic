df main() i64 {
    let a: i64 = 5;
    let b: i64 = 10;
    let c: i64 = 3;
    let max: i64 = a;

    if (b > max) {
        max = b;
    }
    if (c > max) {
        max = c;
    }

    return max;
}
